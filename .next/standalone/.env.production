# Production Environment Variables for PostgreSQL Deployment
# Replace these values with your actual hosting details

# Node.js Environment
NODE_ENV=production

# PostgreSQL Database Configuration
# Format: postgresql://username:password@hostname:port/database_name
# For cPanel with PostgreSQL support or VPS hosting
DATABASE_URL="postgresql://your_username:your_password@localhost:5432/your_database_name"

# Alternative cloud PostgreSQL services:
# Supabase: postgresql://postgres:password@db.project-id.supabase.co:5432/postgres
# Neon: postgresql://username:password@ep-endpoint-name.region.aws.neon.tech/database_name
# Railway: postgresql://postgres:password@containers-us-west-1.railway.app:6543/railway
# Heroku: postgresql://username:password@hostname:5432/database_name

# Application Configuration
PORT=3000
HOSTNAME=0.0.0.0

# Performance Configuration
PGPOOL_MAX_CONNECTIONS=20
PGCONNECT_TIMEOUT=10

# Security (Generate a secure random string)
NEXTAUTH_SECRET="your-32-character-secret-key-here"
NEXTAUTH_URL="https://yourdomain.com"

# PostgreSQL SSL Configuration (for production)
PGSSLMODE=prefer

# Instructions for Production Deployment:
# 1. Choose PostgreSQL hosting service (Supabase, Neon, Railway, etc.)
# 2. Create PostgreSQL database
# 3. Update DATABASE_URL with actual connection string
# 4. Generate a secure NEXTAUTH_SECRET (32+ characters)
# 5. Set your domain in NEXTAUTH_URL
# 6. Run: npx prisma migrate deploy
# 7. Ensure SSL is properly configured for production