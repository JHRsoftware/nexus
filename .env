# DEVELOPMENT Environment Variables
# This file is for local development only

# Database connection is configured in .env.local for development
# .env.local takes priority and contains the Neon PostgreSQL connection
# This file serves as documentation for required environment variables

# DATABASE_URL is set in .env.local with Neon PostgreSQL connection
# Format: postgresql://username:password@host:port/database?sslmode=require&channel_binding=require

# For production deployment, use .env.production file or hosting platform environment variables